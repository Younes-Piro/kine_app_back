import { PlaceholderPage } from '@/components/shared/PlaceholderPage';

export function UserPermissionsPage() {
  return (
    <PlaceholderPage
      title="User Permissions"
      description="Permission assignment UI is planned for the permissions feature step."
    />
  );
}
